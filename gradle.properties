org.gradle.jvmargs=-Xmx2048M
org.gradle.parallel=true

# Minecraft + loader/toolchain versions (all verified against published maven metadata, 2026-06-24)
minecraft_version=26.2
enabled_platforms=fabric,neoforge

archives_base_name=betterbrightness
mod_version=1.0.0
maven_group=io.github.fimkov.betterbrightness

# Architectury API line that targets MC 26.2 (architectury-fabric/-neoforge 21.0.2).
# Verified: architectury-fabric-21.0.2.pom depends on fabric-api 0.152.1+26.2 and fabric-loader 0.19.3.
architectury_api_version=21.0.2

# Fabric
fabric_loader_version=0.19.3
# Latest Fabric API for MC 26.2.
fabric_api_version=0.153.0+26.2

# NeoForge for MC 26.2 (latest published; only beta builds exist for 26.2 so far).
neoforge_version=26.2.0.7-beta

# Forge: UNAVAILABLE for 26.2 (architectury-forge not published). See settings.gradle / API-NOTES.md.
